<%= form_for(@postlet) do |f| %>
  <% if @postlet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postlet.errors.count, "error") %> prohibited this postlet from being saved:</h2>

      <ul>
      <% @postlet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
