<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Rubytrist" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
    <strong>
      <% if current_user && current_user.name.present? %>
        Logged in as: <%= current_user.name %>
        <%= button_to "Logout", "/session", {:method => :delete} %>
      <% end %>
    </strong>
    
    <%= yield %>


    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
